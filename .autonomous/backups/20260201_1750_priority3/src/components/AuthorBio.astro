---
/**
 * Author Bio Component
 * Displays author information for AI Website Builders
 * Builds E-E-A-T (Experience, Expertise, Authority, Trust)
 * Author: Alex Chen, Former Startup CTO
 */

interface Props {
  variant?: 'compact' | 'full';
}

const { variant = 'full' } = Astro.props;

const author = {
  name: 'Alex Chen',
  initials: 'AC',
  role: 'Former Startup CTO',
  location: 'San Francisco',
  years: 10,
  toolsTested: 50,
  companiesBuilt: 7,
  currentFocus: 'Testing website builders so you don\'t waste time'
};
---

<aside class={`border border-indigo-100 bg-gradient-to-br from-indigo-50/50 to-white p-4 sm:p-5 md:p-6 mt-8 sm:mt-10 md:mt-12 rounded-xl shadow-sm ${variant === 'compact' ? 'text-sm' : ''}`} aria-labelledby="author-bio-title">
  <div class="flex flex-col sm:flex-row items-start sm:items-center gap-3 sm:gap-4 text-center sm:text-left">
    <!-- Avatar placeholder - user can replace with real photo -->
    <div
      class="w-12 h-12 sm:w-14 sm:h-14 md:w-16 md:h-16 rounded-full bg-gradient-to-br from-[#4F46E5] to-[#0EA5E9] flex items-center justify-center text-white text-lg sm:text-xl md:text-2xl font-bold flex-shrink-0 mx-auto sm:mx-0 shadow-md"
      role="img"
      aria-label={`${author.name} - Author avatar`}
    >
      {author.initials}
    </div>

    <div class="flex-1">
      <h3 id="author-bio-title" class="font-bold text-base sm:text-lg md:text-xl mb-1 text-gray-900">
        About the Author
      </h3>
      <p class="text-xs sm:text-sm text-indigo-700 mb-2 sm:mb-3">
        Written by <strong>{author.name}</strong> — {author.role}, {author.location}
      </p>

      {variant === 'full' && (
        <>
          <p class="text-gray-700 text-xs sm:text-sm mb-2 sm:mb-3 leading-relaxed">
            Spent {author.years} years building products. {author.companiesBuilt} companies as CTO or technical founder.
            Seen the same mistake repeated: founders waste months on the wrong tool.
          </p>

          <p class="text-gray-700 text-xs sm:text-sm mb-2 sm:mb-3 leading-relaxed">
            <strong>Actually tested {author.toolsTested}+ website builders</strong> — not just read reviews, built real projects with each.
            Some surprised me. Most disappointed. Here's what I learned.
          </p>

          <p class="text-gray-700 text-xs sm:text-sm mb-3 sm:mb-4 leading-relaxed">
            I don't accept affiliate payouts for positive reviews. If a tool sucks, I say so.
            If it's great, I show you exactly why.
          </p>
        </>
      )}

      <div class="flex flex-wrap justify-center sm:justify-start gap-1.5 sm:gap-2 text-[10px] sm:text-xs" role="list" aria-label="Author credentials">
        <span class="px-2 py-1 bg-white border border-indigo-200 text-indigo-700 rounded-md font-medium shadow-sm" role="listitem">
          {author.toolsTested}+ tested
        </span>
        <span class="px-2 py-1 bg-white border border-indigo-200 text-indigo-700 rounded-md font-medium shadow-sm" role="listitem">
          {author.companiesBuilt} startups
        </span>
        <span class="px-2 py-1 bg-white border border-indigo-200 text-indigo-700 rounded-md font-medium shadow-sm" role="listitem">
          {author.location}
        </span>
      </div>

      {variant === 'full' && (
        <a href="/about" class="inline-block mt-3 text-[#4F46E5] hover:text-[#4338ca] underline decoration-2 underline-offset-2 text-xs sm:text-sm font-semibold transition-colors">
          Read my full story →
        </a>
      )}
    </div>
  </div>
</aside>
